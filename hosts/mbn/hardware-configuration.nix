{ ... }:
{
  # On nix-darwin there is no generated hardware-configuration; hardware
  # is managed by macOS. This file is kept for structural parity with the
  # NixOS hosts and can hold darwin-specific disk/system knobs if needed.
}